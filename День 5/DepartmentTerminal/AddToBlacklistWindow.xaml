<Window x:Class="DepartmentTerminal.AddToBlacklistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DepartmentTerminal"
        mc:Ignorable="d"
        Title="Добавление в чёрный список" 
        Height="407" 
        Width="470"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="🚫 Добавление посетителя в чёрный список" 
                   FontSize="16" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,5">
            <StackPanel>
                <TextBlock x:Name="txtVisitorInfo" FontWeight="Bold"/>
                <TextBlock x:Name="txtPassport" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2" Text="Причина добавления:*" FontWeight="Bold" Margin="0,10,0,5"/>
        <TextBox Grid.Row="3" x:Name="txtReason" Height="120" TextWrapping="Wrap" 
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button x:Name="btnAdd" Content="✅ Добавить" Width="120" Height="35" Margin="10"
                    Background="DarkRed" Foreground="White" Click="BtnAdd_Click"/>
            <Button x:Name="btnCancel" Content="❌ Отмена" Width="120" Height="35" Margin="10"
                    Background="Gray" Foreground="White" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
